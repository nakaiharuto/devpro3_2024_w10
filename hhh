void setup() {
  // シリアル通信の開始
  Serial.begin(9600);
}

void loop() {.......
  // アナログピンA0の値を読み取る
  int sensorValue = analogRead(A0);

  // 読み取った値をシリアルモニターに送る
  Serial.println(sensorValue);

  // データのプロットのための短い遅延
  delay(100); // データを見やすくするために短い遅延を入れます
}
